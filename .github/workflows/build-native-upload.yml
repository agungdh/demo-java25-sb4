name: Build Nativa (GraalVM) & Upload (Actions + Release)

on:
  push:
    branches: [ master ]
  release:
    types: [ published ]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    strategy:
      matrix:
        arch: [amd64, arm64]

    # Konsisten 24.04 untuk kedua arsitektur
    runs-on: ubuntu-24.04-${{ matrix.arch }}

    steps:
      # Checkout ke tag release saat event release published
      - name: Checkout (release tag)
        if: github.event_name == 'release'
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.release.tag_name }}

      # Checkout default untuk push ke master / manual
      - name: Checkout (branch)
        if: github.event_name != 'release'
        uses: actions/checkout@v4

      - name: Set up JDK (GraalVM)
        uses: actions/setup-java@v4
        with:
          distribution: 'graalvm'
          java-version: '25'
          cache: 'maven'

      - name: Build native image (skip tests)
        run: |
          set -e
          chmod +x mvnw
          # Propagasi arsitektur ke Maven profiles (pom.xml)
          ./mvnw -Pnative -DskipTests -Darch=${{ matrix.arch }} native:compile

      - name: Package artifact (zip)
        id: pack
        shell: bash
        run: |
          set -euo pipefail
          BINPATH=$(find target -maxdepth 1 -type f -perm -111 -printf "%p %s\n" \
            | sort -k2 -nr | head -n1 | awk '{print $1}')

          if [ -z "${BINPATH:-}" ] || [ ! -f "$BINPATH" ]; then
            echo "Native binary tidak ditemukan di target/." >&2
            ls -lah target || true
            exit 1
          fi

          APPNAME=$(basename "$BINPATH")

          # Suffix: tag saat release, atau run_id saat push/master
          if [[ "${GITHUB_EVENT_NAME}" == "release" ]]; then
            SUFFIX="${{ github.event.release.tag_name }}"
          else
            SUFFIX="${GITHUB_RUN_ID}"
          fi

          ZIPNAME="${APPNAME}-linux-${{ matrix.arch }}-${SUFFIX}.zip"

          echo "Jalankan: ./${APPNAME}" > target/HOW_TO_RUN.txt
          (cd target && zip -r "../${ZIPNAME}" "$APPNAME" HOW_TO_RUN.txt >/dev/null)

          sha256sum "${ZIPNAME}" | tee "${ZIPNAME}.sha256"

          echo "zip=${ZIPNAME}" >> "$GITHUB_OUTPUT"
          echo "checksum=${ZIPNAME}.sha256" >> "$GITHUB_OUTPUT"

      # Simpan di Actions artifacts
      - name: Upload artifact (Actions tab)
        uses: actions/upload-artifact@v4
        with:
          name: ${{ steps.pack.outputs.zip }}
          path: |
            ${{ steps.pack.outputs.zip }}
            ${{ steps.pack.outputs.checksum }}
          if-no-files-found: error
          retention-days: 14

      # Saat release published, upload juga ke halaman Release (per-arch)
      - name: Upload assets to Release
        if: github.event_name == 'release'
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.event.release.tag_name }}
          files: |
            ${{ steps.pack.outputs.zip }}
            ${{ steps.pack.outputs.checksum }}
          generate_release_notes: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
